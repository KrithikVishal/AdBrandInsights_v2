# Environment Configuration Example
# Copy this file to .env and update the values

# Application Environment
NODE_ENV=development
PORT=5000

# Database Configuration (Optional - uses in-memory storage if not provided)
DATABASE_URL=postgresql://username:password@localhost:5432/admybrand_insights

# Development Settings
VITE_APP_NAME=ADmyBRAND Insights
VITE_APP_VERSION=1.0.0

# Production Settings (for Vercel deployment)
# Add these in your Vercel dashboard environment variables
# DATABASE_URL=your_production_database_url
# Any additional API keys or secrets needed